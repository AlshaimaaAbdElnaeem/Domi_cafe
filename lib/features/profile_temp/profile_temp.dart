import 'package:flutter/material.dart';

class ProfileTemp extends StatelessWidget {
  const ProfileTemp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      ""
    );
  }
}