import 'package:flutter/material.dart';

class AppColors {
  // -----------------------------
  // Light Theme Colors
  // -----------------------------
  static const lightBackground = Color(0xFFF7F7F7);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightText = Color(0xFF1A1A1A);
  static const lightHeading = Color(0xFF2C2C2C);
  static const lightInput = Color(0xFFFFFFFF);
  static const lightInputBorder = Color(0xFFD1D5DB);
  static const lightPrimary = Color(0xFFB45309);
  static const lightPrimaryHover = Color(0xFF92400E);

  // -----------------------------
  // Dark Theme Colors
  // -----------------------------
  static const darkBackground = Color(0xFF0F0F12);
  static const darkSurface = Color(0xFF1A1A1E);
  static const darkText = Color(0xFFE5E5E5);
  static const darkHeading = Color(0xFFFACC15);
  static const darkInput = Color(0xFF2A2A2E);
  static const darkInputBorder = Color(0xFF3F3F46);
  static const darkPrimary = Color(0xFFB45309);
  static const darkPrimaryHover = Color(0xFFD97706);

  // -----------------------------
  // Shared Colors
  // -----------------------------
  static const white = Color(0xFFFFFFFF);
  static const black = Color(0xFF000000);
  static const transparent = Colors.transparent;
}
